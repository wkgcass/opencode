# 当前分支改动说明与手动测试清单

## 统计范围

- 基准提交：`17544802c38a4d35834275526ccf38be1cdcfbf4`
- 统计规则：包含基准提交之后、截至 `d1b946c6793505edceae8833e7511efe53487429` 的全部 22 个非合并提交；不包含基准提交本身。
- 主要影响范围：桌面版工作区与皮肤、共享 App 的会话时间线和标签栏。皮肤及 Codex 风格工作区均以桌面标记隔离，不应改变 Web 版原有布局。

## 功能改动摘要

### 1. Codex 风格的桌面工作区

- 桌面版改为固定工作区结构：顶部 40px 标题栏、左侧 262px 项目/会话侧栏、右侧主内容区。
- 标题栏、原生 Windows 窗口按钮区域和应用背景使用一致的明暗/皮肤颜色，避免窗口右上角出现白色接缝。
- 标题栏标签和按钮统一为更紧凑的尺寸、圆角、间距及悬停效果；开发渠道标记在桌面版隐藏。
- 首页、新建任务页、会话页统一为左上角 24px 圆角、其余边缘贴合工作区的面板造型，并去除旧版外边距和高架阴影。
- 桌面窗口宽度低于 `lg` 断点时隐藏固定侧栏；Web 版继续使用原布局。
- 开发环境默认不再显示 Debug Bar，只有显式设置 `VITE_ENABLE_DEBUG_BAR=1` 时才显示。

### 2. 桌面新建任务页

- 新建任务页改为“中央项目提示 + 底部输入框”的布局。
- 中央提示会显示当前草稿/工作区的真实项目名；简体中文、繁体中文和英文有对应文案。
- 输入框改为较大的悬浮卡片，隐藏 provider tip，但保留原有模型、Agent、工作区选择和提交能力。
- `YU7 GT` 与 `SU7 Ultra` 使用各自的赛道主题提示语。

### 3. 可配置且持久化的皮肤系统

- 设置页新增“皮肤”选项，传统设置页和 V2 设置页均支持。
- 可选皮肤：

  | 皮肤 | 外观与重点元素 |
  | --- | --- |
  | `OpenCode` | 使用普通 OpenCode 主题，可自行设置明暗模式和主题。 |
  | `Miku Future` | 默认桌面皮肤；浅色青、粉、紫渐变，点阵背景，`Miku Future skin ♫` 品牌字样，彩色输入框边框。 |
  | `Emily` | 浅色草地、象牙白、苔绿和淡粉配色；新建页使用 Emily 图片背景及半透明 Field Notes 卡片。 |
  | `YU7 GT` | 深色赛道座舱，红色强调；新建页使用 YU7 赛道图，选中项带方格旗，标题栏活动标签带红色计时线。 |
  | `SU7 Ultra` | 深色赛道座舱，亮黄色强调；新建页使用 SU7 赛道图，选中项带黄色速度标记，标题栏活动标签带黄色计时线。 |

- 皮肤选择写入本地存储，重启后保持；启动预加载会尽早恢复背景、标题栏和颜色模式，减少首屏闪烁。
- 皮肤 CSS 按需加载并与桌面工作区隔离。
- 除 `OpenCode` 外，每个皮肤会强制使用其指定的颜色模式和 `oc-2` 主题；此时设置页和命令面板中的颜色模式/主题切换被禁用。切回 `OpenCode` 后恢复可编辑。
- 新增桌面皮肤开发说明，约定目录、注册、样式隔离、资源和验证方式。

### 4. 项目与会话侧栏

- 侧栏展示所有已连接服务器的项目，不再仅显示当前/本地服务器：
  - 本地项目使用文件夹图标。
  - 远程项目使用云朵图标。
- 每个项目展示最近的最多 6 个根会话；点击项目返回首页，点击会话打开并选中对应标签。
- 顶部“新建任务”使用当前选择的项目；悬停项目时显示该项目专用的新建按钮。
- 当前项目或会话有明确的选中背景/强调色，各皮肤提供对应视觉效果。
- 项目可在同一服务器分组内拖拽排序；会话可在所属项目内拖拽排序。
- 会话顺序按服务器作用域和项目路径持久化；刷新、重启、分页加载或出现新会话时尽量保留用户顺序。项目顺序通过原有项目存储持久化。
- 拖拽时显示由当前皮肤着色的“圆点 + 横线”插入位置指示器。

### 5. 侧栏删除会话

- 悬停会话时显示垃圾桶按钮。
- 点击后显示带会话标题的确认对话框，支持取消和危险样式的删除按钮；请求期间按钮进入加载/禁用状态。
- 删除成功后立即从侧栏及本地同步状态移除目标会话和全部子会话，并关闭关联标签。
- 删除失败时保留会话和对话框，并显示错误 Toast。

### 6. 会话运行进度环

- 正在工作的会话在标题栏标签头像位置显示旋转进度环；悬停标签时仍可查看项目头像。
- 同一会话在桌面侧栏右侧也显示 14px 进度环；悬停会话时进度环隐藏，以便显示删除按钮。
- 开启“减少动态效果”时进度环不旋转，改为静态角度。

### 7. 可折叠推理面板

- Assistant 的 reasoning 内容改为可折叠面板：
  - 流式生成时显示“思考中”并自动展开。
  - 推理结束后显示“已思考”并自动折叠。
- 展开内容区最大高度 350px，拥有独立滚动条。
- 流式推理默认跟随最新内容；用户向上滚动、拖动滚动条或使用键盘浏览后暂停自动跟随，回到底部后恢复。
- 展开历史推理时会重新测量虚拟时间线并适度调整主滚动区，避免面板被视口裁切。

### 8. 实时 Shell 输出

- Shell 工具运行中一旦产生输出便自动展开，并实时追加已去除 ANSI 控制码的输出。
- 输出区默认自动跟随到底部；用户向上滚动、拖动或键盘浏览后暂停跟随，回到底部后恢复。
- 命令结束后，若本次工具曾处于运行状态，面板自动折叠；折叠状态仍显示命令摘要。
- 保留手动展开、滚动和复制完整命令/输出的能力。

### 9. 上下文占用进度环

- 会话上下文用量环改为更醒目的配色：浅色模式使用蓝色，深色模式使用黄色。
- 同时覆盖普通与 V2 进度环，保留背景轨道和 Tooltip 中原有的用量详情。

### 10. 服务器暂不可用时的恢复体验

- 打开尚未注册/仍在启动的目标服务器会话时，不再隐藏或立即清理标签，而是显示“正在连接…”标签和带 Spinner 的等待页。
- 服务器连接可用后，原标签自动加载目标会话。
- 请求出现临时 `Failed to fetch` 时显示“暂时无法连接”页面及“重试”按钮，而非通用错误页。
- 通知状态在当前目标服务器不可用时回退到默认服务器，避免页面因通知服务初始化失败而崩溃。
- WSL 服务器列表尚未加载时不会误删已持久化标签和最近关闭标签。
- 删除远程服务器连接前先关闭其菜单，避免删除后遗留不可见的 dismissal layer，造成页面无法点击。

### 11. 标题栏长标签处理

- 标签标题过长时使用省略号截断。
- 标签处于活动、悬停或关闭按钮键盘聚焦状态时，为右侧关闭按钮预留空间，避免文字与关闭按钮重叠。

## 手动测试准备

- 使用桌面版启动应用，准备至少 2 个本地项目，每个项目至少 7 个根会话。
- 至少一个项目中准备：
  - 一个能够持续输出的 Shell 命令。
  - 一个会产生较长 reasoning 内容的模型请求。
  - 一个运行时间足够观察进度状态的请求。
- 如需测试远程能力，连接一个 HTTP 或 WSL 服务器并在其上准备项目和会话。
- 分别在浅色、深色系统环境以及启用“减少动态效果”的环境下执行相关用例。
- 皮肤默认值受已有本地存储影响；验证首次启动时应使用干净的应用数据，其他用例可直接从设置中切换皮肤。

## 手动测试用例

### MT-01 桌面工作区基础布局

**步骤**

1. 启动桌面版并最大化窗口。
2. 依次进入首页、新建任务页和任意会话。
3. 缩窄窗口至 `lg` 断点以下，再恢复宽窗口。

**目视检查**

- 标题栏高度为 40px，原生窗口按钮区域与标题栏同色，没有白边或断层。
- 宽窗口左侧出现约 262px 固定侧栏，右侧为完整主工作区；窄窗口侧栏隐藏且主内容无横向溢出。
- 首页、新建页、会话页的主面板均为左上角圆角、其他边贴合工作区，边框连续且没有旧版 8px 外边距/高架阴影。
- 桌面标题栏不显示 `DEV`/`BETA` 渠道徽标。
- 页面切换时标题栏、侧栏和主面板没有明显抖动或颜色闪白。

### MT-02 新建任务页布局、项目名和语言

**步骤**

1. 从侧栏选择项目 A，点击顶部“新建任务”。
2. 从项目 B 行右侧的悬停按钮新建任务。
3. 分别切换简体中文、繁体中文和英文。

**目视检查**

- 页面中央显示终端形状图标和提示语，带下划线的项目名分别正确显示 A、B，而不是固定的 `opencode`。
- 简体中文为“我们应该在 … 中构建什么？”，繁体中文和英文文案匹配各自语言。
- 输入框位于页面底部中央，卡片边框、圆角和阴影完整，没有贴边或被裁切。
- provider tip 不显示；模型、Agent、工作区选择器及发送按钮仍存在且布局正常。

### MT-03 皮肤列表、切换与持久化

**步骤**

1. 打开设置的常规/外观区域。
2. 依次选择 `OpenCode`、`Miku Future`、`Emily`、`YU7 GT`、`SU7 Ultra`。
3. 每次切换后观察标题栏、侧栏、首页、新建页和会话页。
4. 保持任一非 OpenCode 皮肤并重启应用。

**目视检查**

- 两种设置界面中的皮肤选择器均列出 5 个选项，切换后立即生效。
- 标题栏原生按钮符号在浅色皮肤下为深色、深色皮肤下为浅色，且按钮区域背景与皮肤相连。
- 侧栏品牌、活动标签、选中项目/会话、面板、输入框和背景统一切换，没有残留上一皮肤的颜色或图片。
- 重启后仍是关闭前的皮肤，首屏没有长时间显示错误主题或明显白闪。

### MT-04 各皮肤关键视觉

**步骤**

1. 对每个皮肤打开新建任务页。
2. 选中一个项目和一个会话，并悬停标题栏标签、侧栏项目和会话。

**目视检查**

- `OpenCode`：普通 OpenCode 配色，选中项仍有清晰的轻量背景高亮。
- `Miku Future`：浅青背景、青粉紫渐变/点阵、`Miku Future skin ♫` 字样、青粉渐变输入框边框和活动标签底线。
- `Emily`：草地插画完整覆盖新建页，中央半透明 Field Notes 卡片可读；侧栏为苔绿/象牙白/淡粉配色。
- `YU7 GT`：深色 YU7 赛道图、红色提示和输入框描边、`TRACK MODE` 文案；选中侧栏项右侧有方格旗，悬停时旗帜让位于操作按钮。
- `SU7 Ultra`：深色 SU7 赛道图、黄色提示和输入框描边、`PROTOTYPE` 文案；选中侧栏项右侧有黄色速度标记，悬停时标记让位于操作按钮。
- YU7/SU7 使用各自赛道文案；在较窄窗口中图片焦点和提示文字仍可读，输入框不遮挡关键内容。

### MT-05 皮肤对主题设置的约束

**步骤**

1. 选择任一非 `OpenCode` 皮肤。
2. 打开设置页并打开命令面板，尝试修改主题和颜色模式或使用相应快捷键。
3. 切回 `OpenCode`，再次修改主题和颜色模式。

**目视检查**

- 非 OpenCode 皮肤下，主题和颜色模式控件呈禁用状态；相关命令不可执行，皮肤指定的明暗模式不会被快捷键破坏。
- `Miku Future`、`Emily` 保持浅色；`YU7 GT`、`SU7 Ultra` 保持深色。
- 切回 `OpenCode` 后控件恢复可用，主题/明暗切换立即正常生效。

### MT-06 本地与远程项目侧栏

**步骤**

1. 同时连接本地和远程服务器。
2. 展开观察所有服务器上的项目，分别点击本地项目、远程项目及其会话。
3. 检查一个拥有 7 个以上根会话的项目。

**目视检查**

- 本地和远程项目均出现；本地项目使用文件夹图标，远程项目使用云朵图标。
- 每个项目最多显示 6 个根会话，子 Agent 会话不作为独立根会话重复出现。
- 选中的项目或会话有明确高亮，切换服务器后高亮不会错误落到同路径/同名的另一服务器项目。
- 长项目名和长会话名以省略号截断，不挤压右侧操作区。

### MT-07 侧栏新建任务入口

**步骤**

1. 选中项目 A，点击侧栏顶部“新建任务”。
2. 悬停项目 B，点击其右侧编辑图标。
3. 对远程项目重复第 2 步。

**目视检查**

- 项目行未悬停时操作按钮隐藏，悬停或键盘聚焦时平滑出现。
- 顶部入口在项目 A 创建草稿；项目行入口在对应的 B/远程项目创建草稿。
- 新草稿的活动高亮、中央项目名和输入框工作区选择保持一致。

### MT-08 项目和会话拖拽排序

**步骤**

1. 在同一服务器分组内把一个项目拖到另两个项目之间。
2. 在同一项目中把一个会话拖到列表顶部、列表中间和底部。
3. 尝试拖到原位置、拖出有效区域，以及从操作按钮开始拖动。
4. 刷新并重启应用，再创建一个新会话。

**目视检查**

- 拖动源半透明；有效位置出现“圆点 + 横线”插入指示器，颜色和光效符合当前皮肤。
- 放下后项目/会话顺序准确，没有跳一位、重复或丢失。
- 原位置和无效区域不产生错误排序；点击新建/删除按钮不会误触拖拽。
- 刷新、重启后顺序保留；新会话出现时，已有手工顺序仍保留且新会话进入合理位置。
- 不同服务器和不同项目的会话顺序互不污染。

### MT-09 侧栏删除会话

**步骤**

1. 悬停某会话并点击垃圾桶，先选择“取消”。
2. 再次打开确认框并确认删除。
3. 对包含子 Agent 会话、且根会话和子会话均已打开标签的会话重复删除。
4. 在断网或让删除 API 返回失败时再测试一次。

**目视检查**

- 垃圾桶只在悬停/聚焦时出现，确认框显示正确会话标题，取消后列表不变。
- 确认删除时按钮显示加载状态且不可重复点击；成功后对话框关闭、侧栏行消失。
- 根会话及其所有子会话标签一并关闭，不残留“未知会话”标签。
- 删除失败时会话仍在，确认框可继续操作，并出现可读的错误 Toast。

### MT-10 会话运行进度环

**步骤**

1. 启动一个持续数秒的会话请求。
2. 同时观察标题栏标签和侧栏中的对应会话。
3. 分别悬停标签和侧栏会话；请求完成后继续观察。
4. 开启系统“减少动态效果”后重复。

**目视检查**

- 运行中，标题栏头像位置和侧栏右侧同时出现细线旋转环，尺寸清晰且不挤压文字。
- 悬停标签时进度环切换为项目头像；悬停侧栏会话时进度环隐藏并显示删除按钮。
- 请求完成后两个进度环及时消失并恢复正常头像/行状态。
- 减少动态效果模式下进度环静止，不持续旋转。

### MT-11 推理折叠面板

**步骤**

1. 发起会持续输出较长 reasoning 的请求。
2. 生成中观察面板，并在内容区向上滚动、拖动滚动条、按 `PageUp`，随后滚回底部。
3. 推理完成后手动展开、折叠历史 reasoning。

**目视检查**

- 流式阶段标题为“思考中”并带动态效果，面板默认展开且跟随最新内容。
- 内容区高度不超过 350px，使用独立滚动条；向上浏览后不会被新内容强制拉到底部，回到底部后恢复跟随。
- 推理结束后标题变为“已思考”并自动折叠。
- 手动展开历史内容时，时间线重新排布稳定，触发器和内容不会被顶部标题或底部视口裁切；折叠后不留下大块空白。

### MT-12 实时 Shell 输出

**步骤**

1. 执行一个每隔短时间输出一行、总输出超过容器高度的 Shell 命令。
2. 运行中向上滚动、拖动滚动条或按 `Home`，等待新输出后滚回底部。
3. 等命令结束，再手动展开并使用复制按钮。

**目视检查**

- 第一段输出到达后工具面板自动展开，命令和输出逐行实时增加，ANSI 颜色控制字符不以乱码显示。
- 默认自动滚到底部；用户向上浏览后视口保持原位置，回到底部后恢复跟随。
- 命令完成后面板自动折叠，折叠行仍显示命令摘要。
- 手动展开可看到完整最终输出；复制状态反馈正常，复制内容含命令与输出。

### MT-13 上下文用量环

**步骤**

1. 打开存在上下文用量数据的会话。
2. 分别使用浅色和深色外观，观察普通布局和 V2 布局中的上下文用量入口。
3. 悬停查看 Tooltip。

**目视检查**

- 浅色模式进度弧为蓝色、深色模式为黄色，背景轨道与进度弧有足够对比。
- 环形百分比随会话用量变化，没有全满、反向或不可见的异常。
- Tooltip 仍显示原有上下文用量明细，环与周围按钮垂直居中。

### MT-14 服务器启动中、断线与恢复

**步骤**

1. 保留一个远程/WSL 会话标签并关闭对应服务器，然后重启应用。
2. 打开该标签，随后启动并重新注册服务器。
3. 在已打开会话中让请求触发 `Failed to fetch`，点击“重试”。
4. 在 WSL 列表尚未加载完成的启动阶段观察已保存标签。

**目视检查**

- 标签不消失，标题显示“正在连接…”，内容区显示 Spinner、服务器名和等待说明。
- 服务器可用后同一标签自动加载目标会话，不额外创建重复标签。
- 临时断线显示专用“暂时无法连接”页面和“重试”按钮，而不是空白页或通用崩溃页。
- 恢复连接并重试后会话正常显示；标题栏、未读状态和通知逻辑没有报错或闪退。
- WSL 列表加载前，已保存标签和最近关闭记录不会被提前清理。

### MT-15 删除远程服务器连接后的交互

**步骤**

1. 打开远程服务器行的更多菜单。
2. 点击删除服务器连接。
3. 删除完成后点击设置页空白处、其他按钮和主页面。

**目视检查**

- 菜单先正常关闭，服务器行再消失。
- 页面上没有残留透明遮罩；鼠标和键盘仍能操作其他控件。
- 对应远程项目从侧栏消失，其他服务器的项目和顺序不受影响。

### MT-16 标题栏长标签与关闭按钮

**步骤**

1. 打开多个标题很长的会话和草稿标签。
2. 分别观察普通、悬停、活动状态，并用键盘将焦点移到关闭按钮。
3. 点击关闭按钮。

**目视检查**

- 长标题以省略号结尾，不横向溢出标签。
- 悬停、活动或关闭按钮聚焦时，标题右侧自动留出空间，不与 `×` 重叠。
- 关闭按钮始终完整可见、可点击，其他标签不会发生明显跳位。

### MT-17 Debug Bar 显示条件

**步骤**

1. 在开发环境不设置 `VITE_ENABLE_DEBUG_BAR` 启动应用。
2. 设置 `VITE_ENABLE_DEBUG_BAR=1` 后重启。

**目视检查**

- 默认情况下新旧布局都不显示 Debug Bar。
- 显式启用后 Debug Bar 正常出现，且不遮挡标题栏、侧栏或主内容。

### MT-18 Web 版隔离回归

**步骤**

1. 使用同一 `packages/app` 构建/启动 Web 版。
2. 访问首页、新建任务页和会话页，并打开设置。

**目视检查**

- Web 版不出现桌面固定侧栏、赛道/Emily 图片、桌面标题栏覆盖或桌面面板圆角规则。
- 新建任务页仍保持 Web 原设计。
- 皮肤默认是 `OpenCode`，桌面皮肤不会无故覆盖 Web 背景与窗口结构。

## 提交对应表

| 提交 | 标题 | 对应改动 |
| --- | --- | --- |
| `d154ddae2` | `feat(desktop): adopt codex-style workspace` | Codex 风格桌面框架、标题栏、侧栏、新建任务页和 Windows 标题栏颜色。 |
| `be2a0b0ca` | `feat(desktop): add configurable skins` | 皮肤注册、设置入口、持久化、预加载和 Miku Future。 |
| `0f68449b3` | `fix(desktop): align skin color schemes` | 皮肤与系统/窗口明暗配色对齐。 |
| `d0ce70645` | `fix(app): hide debug bar by default` | Debug Bar 改为显式环境变量开启。 |
| `a31f167d1` | `feat(desktop): add Emily skin` | Emily 皮肤、图片资源、皮肤目录与按需样式加载。 |
| `6b01089b9` | `refactor(desktop): isolate workspace customization` | 桌面工作区、侧栏、新建页、设置选择器及皮肤构建逻辑模块化隔离。 |
| `1e73bd1d6` | `docs(desktop): add skin customization guide` | 新增皮肤开发说明。 |
| `086a01038` | `fix(desktop): align home panel border` | 首页与新建/会话面板边框、圆角和边距统一。 |
| `b17738b9d` | `fix(desktop): enforce skin appearance` | 皮肤强制主题/明暗模式并锁定主题设置。 |
| `1ed91b9b4` | `feat(desktop): add YU7 GT skin` | YU7 GT 深色赛道皮肤及资源。 |
| `4c9558315` | `feat(desktop): add SU7 Ultra skin` | SU7 Ultra 深色赛道皮肤及资源。 |
| `2c6c281a0` | `feat(desktop): add sidebar session deletion` | 侧栏会话删除、确认、级联清理和失败提示。 |
| `3553673bc` | `feat(app): persist desktop sidebar ordering` | 项目/会话拖拽排序与会话顺序持久化。 |
| `370c7c63e` | `feat(desktop): theme sidebar drop indicators` | 各皮肤专属拖拽插入指示器。 |
| `f1f7e031f` | `fix(desktop): highlight selected sidebar items` | OpenCode 皮肤下当前项目/会话高亮。 |
| `ce2e08eb9` | `feat(desktop): add session progress rings` | 标题栏和侧栏的会话运行进度环。 |
| `08bb56bff` | `fix(desktop): show remote projects in sidebar` | 展示全部服务器项目、远程云图标及远程状态安全处理。 |
| `7c0948eb4` | `feat(app): add collapsible reasoning panel` | 可折叠推理面板、独立滚动和自动跟随控制。 |
| `632a983bf` | `fix(app): highlight context usage ring` | 上下文占用环的蓝/黄高亮配色。 |
| `a87c5c5be` | `fix(app): handle unavailable notification server` | 服务器等待/重试页、标签保留、通知回退及 WSL 启动恢复。 |
| `87c86a8a0` | `fix(app): truncate tab titles beside close button` | 长标签省略号及关闭按钮避让。 |
| `d1b946c67` | `fix(app): show live shell output` | Shell 输出实时展开、滚动跟随和完成后折叠。 |
