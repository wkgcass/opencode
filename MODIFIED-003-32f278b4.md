# 基于 32f278b4 的魔改汇总与手动测试清单

## 统计范围

- 上游共同基准：`32f278b48f1a495611165d8a9f1ace0b512933e2`。
- 原始提交范围：不包含 `f1495684defa6278fb26201e1b8658609e952246`，包含其后的 `1db6c8850198a75db277cc05e04039c7e4bfa2df`，截至 `3b4e089be384973a40f9a3ddcd815f0964ab55dc`，共 17 个非合并提交。
- `MODIFIED-001-17544802.md` 与 `MODIFIED-002-ff0382e9.md` 记录了早期拆分阶段；拉平到最新 `dev` 后，由本文档统一说明最终保留的行为。
- 历史整理后，本文档由独立 docs 提交承载；上述范围的最终代码差异、重复修改后的最终状态及上游兼容调整由紧随其后的单个功能提交承载。
- 主要影响范围：桌面工作区与皮肤、项目/会话导航、会话时间线、Prompt 与跟进队列、消息 ID、通知提醒、终端交互、服务器恢复和桌面构建。

## 功能改动摘要

### 1. Codex 风格桌面工作区

- 桌面版使用固定顶部标题栏、项目/会话侧栏和主内容区；首页、新建任务页与会话页共享一致的圆角面板边界。
- 桌面布局和样式由平台标记隔离，小窗口会隐藏固定侧栏，Web 版继续使用上游布局。
- 标题栏、Windows 原生窗口按钮、侧栏和主面板会随当前主题或皮肤协调配色，避免接缝、透明色带和按钮区域突变。
- 开发环境仅在显式设置 `VITE_ENABLE_DEBUG_BAR=1` 时显示 Debug Bar。

### 2. 可配置皮肤系统

- 设置页与 V2 设置页均可选择皮肤，选择结果持久化并在启动早期恢复，以减少首屏闪烁。
- 内置 `OpenCode`、`Miku Future`、`Emily`、`YU7 GT`、`SU7 Ultra` 与 `DeepSeek Harness`。
- 非 OpenCode 皮肤可固定自己的明暗模式、主题、标题栏变量、新建任务页提示与背景；切回 OpenCode 后恢复普通主题控制。
- 皮肤 CSS 按需加载，并通过桌面根标记隔离；皮肤目录包含注册、资源、样式约束和验证说明。
- DeepSeek Harness 使用独立 Logo、深色蓝色视觉和品牌化新建任务页，同时保留完整的会话、模型和工作区操作能力。

### 3. 项目、会话与标签导航

- 桌面侧栏展示全部已连接服务器的项目以及每个项目最近的根会话；本地项目使用文件夹图标，远程项目使用由服务器 key 稳定着色的云图标。
- 项目和会话支持作用域内拖拽排序并持久化；分页、刷新和新增会话会尽量保持已有顺序。
- 会话可从侧栏删除；成功后同步移除子会话和关联标签，失败时保留原状态并显示错误。
- 运行中的会话在侧栏和标题栏显示进度环；开启“减少动态效果”后停止旋转动画。
- 标题栏标签支持滚轮横向滚动、拖动滚动条与溢出菜单定位；标签宽度和关闭按钮保持稳定，不再依赖覆盖滚动容器的旧 CSS。

### 4. 新建任务页与 Composer 控件

- 桌面新建任务页使用居中的项目提示和底部 Composer；面板边界与主工作区一致，各皮肤可以提供专属提示和背景。
- V2 新建任务 Composer 保留稳定的 `data-component="session-new-composer"` 挂载点。
- Slash 命令建议补充说明文本，并允许调用方控制建议区域位置，使传统与 V2 Composer 的弹层不遮挡输入区。
- Agent、模型和变体控件按可用项显示；忙碌状态下控件保持明确的禁用或加载反馈。
- 已存在的 V2 会话切换 Agent 或模型时调用对应端点，采用乐观更新；失败时仅在没有更新选择覆盖它的情况下回滚并提示错误。V1 和新建草稿保持兼容。

### 5. 跟进消息投递模式

- 设置页重新提供“引导”和“排队”两种跟进模式，并在传统设置与 V2 设置中保持一致。
- 引导模式在当前运行的安全边界注入后续输入；排队模式等当前工作结束后再投递。
- Prompt 构建、提交路径和界面提示统一使用当前设置，避免多个入口产生不同的投递行为。

### 6. 客户端消息 ID

- 客户端消息 ID 由目标服务器会话统一生成，普通 Prompt、自定义命令和跟进消息共享同一单调序列。
- 生成器观察实时消息与分页历史；服务器出现更新 ID 后，下一条客户端消息会排在其后。
- 扩展时间戳 ID 使用单个下划线分隔时间部分和序列部分，同时兼容纯时间戳及来自服务器的更长 ID。
- 会话删除时释放对应 ID 状态，多个会话之间互不污染。

### 7. 完整会话 Fork

- Fork 对话框支持只复制到指定消息或复制完整会话；完整 Fork 不再要求必须先选中一条消息。
- 对话框明确展示两种模式，保留目标项目/服务器上下文，并在成功后导航到新会话。
- 新增文案在所有 App 语言字典中保持键完整。

### 8. 已完成工作折叠与压缩时间线

- 会话时间线会把较早且已完成的工作分组折叠，在组头显示消息数量和累计工作时长；当前活动区域保持展开。
- 折叠状态不改变原消息顺序、锚点、虚拟列表测量和子 Agent 导航；用户可按组展开历史。
- 压缩事件按真实边界投影，区分压缩前历史、压缩过程和压缩后的当前工作，避免重复、丢失或把当前消息错误折叠。
- 时间线在重载、流式更新、子会话返回和无摘要压缩场景下保持稳定。

### 9. Reasoning、Shell 与上下文生命周期

- Reasoning 使用可折叠面板：流式开始时展开并自动跟随，生成正文后摘要自动收起；用户主动展开或滚动时不会被后续增量强制覆盖。
- 流式 reasoning 的标题、预览和正文状态分离，避免内容增长时摘要反复展开或闪烁。
- Shell 运行时实时展示输出，用户可暂停自动跟随；完成后按生命周期折叠，后续渲染不会丢失折叠状态。
- Shell 长输出保留原生滚动条，复制按钮避让滚动条。
- 引用计数上下文延迟释放，短时间内重新挂载会复用仍然有效的资源；最后一个消费者离开后才执行清理。

### 10. 未读完成提醒

- 会话在后台完成时可发送桌面通知，并可选通过 Bark 发送移动端提醒。
- 设置页支持开关桌面提醒、Bark 提醒和配置 Bark 地址；主进程负责校验并发送请求，renderer 不直接持有网络实现。
- 只有用户尚未查看的完成事件触发提醒；正在查看、已读、重复完成事件和不满足会话状态的事件会被抑制。
- 提醒调度器合并短时间内的状态变化，应用退出时清理挂起任务。

### 11. 终端、窗口与会话细节

- 新建终端后立即聚焦终端输入；终端聚焦期间，全局快捷键不截获应交给 PTY 的组合键。
- ghostty-web 内部焦点切换不会误停光标；焦点离开后停止闪烁并隐藏宿主 caret，避免左上角出现额外光标。
- 桌面主窗口关闭 Chromium 后台节流，窗口失焦后终端输出、流式会话和计时任务继续运行。
- 新建任务页的主面板与标题栏、侧栏边界对齐，皮肤背景不会溢出圆角。

### 12. 服务器恢复、构建与兼容性

- 服务器不可用页面重试时重建目标服务器上下文并重新探测 V1/V2 协议，其他服务器状态不受影响。
- 本地版本识别同时考虑构建渠道和 `local` 版本，避免开发构建误走正式安装逻辑。
- 桌面渠道统一使用 prod 图标；Electron 打包允许复用本地 zip 缓存；主窗口主题同步到原生窗口控件。
- CLI 与 opencode 生成器共享 models 快照缓存，支持本地快照、缓存目录、自定义 URL 和强制刷新。
- App 与 UI 新增文案在全部语言字典中保持键一致；尚未翻译的值安全回退为可读文本。

## 手动测试准备

- 使用桌面版启动应用，准备本地与远程 V2 服务器各一个；如需验证兼容性，再准备一个 V1 服务器。
- 准备至少两个项目、多个有历史消息的会话、可 Fork 的长会话，以及支持多 Agent、模型和变体的会话。
- 准备可产生长输出的 Shell、交互式终端程序、Bark 测试地址和可模拟断线/端点失败的环境。
- 分别在浅色、深色、减少动态效果和六种皮肤下检查关键界面。

## 手动测试用例

### MT-01 桌面工作区与 Web 隔离

1. 在桌面版依次打开首页、新建任务页和会话页，并缩小窗口到 `lg` 断点以下。
2. 在 Web 版打开同样页面。

**目视检查**

- 桌面版标题栏、侧栏和主面板边界连续；窄窗口隐藏固定侧栏。
- Web 版没有桌面固定侧栏、圆角工作区或皮肤布局泄漏。

### MT-02 皮肤切换与持久化

1. 依次切换六种皮肤，检查首页、新建任务页、会话页和设置页。
2. 重启应用，再切回 OpenCode 并切换主题/明暗模式。

**目视检查**

- 每种皮肤的背景、标题栏、强调色和品牌元素匹配，DeepSeek Logo 与提示正常显示。
- 重启后选择保持且无明显首屏闪烁；OpenCode 下主题控制恢复可用。

### MT-03 侧栏排序、删除与远程项目

1. 连接两个远程服务器，拖拽项目和会话后重启。
2. 删除一个含子会话的根会话，并模拟一次删除失败。

**目视检查**

- 排序保持；远程云图标按服务器稳定着色。
- 成功删除同步关闭标签并移除子会话；失败时保留状态并显示错误。

### MT-04 标签栏滚动与会话进度

1. 打开足够多的标签造成溢出，使用滚轮、拖动和溢出菜单定位。
2. 让后台标签运行，并切换“减少动态效果”。

**目视检查**

- 标签可完整访问，滚动位置稳定，关闭按钮不跳动。
- 侧栏与标签显示同一运行状态；减少动态效果时进度环静止。

### MT-05 新建任务与 Slash 建议

1. 在各皮肤中新建任务，切换项目、Agent、模型和变体。
2. 输入 `/` 并通过键盘选择带说明的命令。

**目视检查**

- Composer 位于正确边界内，所有选择器和提交功能可用。
- Slash 建议不遮挡输入框，说明文本和键盘导航正常。

### MT-06 Agent/模型切换失败回滚

1. 在已有 V2 会话切换 Agent、模型和变体并刷新。
2. 让切换端点失败；在第一次请求未完成前再选择另一个值。

**目视检查**

- 成功选择被服务器持久化；失败显示错误并只回滚仍对应旧请求的选择。
- V1 会话和新建草稿不调用不存在的独立切换端点。

### MT-07 引导与排队跟进

1. 会话运行时分别选择“引导”和“排队”并提交多条跟进。
2. 等待当前运行到安全边界和空闲边界。

**目视检查**

- 引导消息在下一安全边界进入，排队消息等当前工作结束后进入。
- 设置页、Composer 提示和实际提交模式一致。

### MT-08 消息 ID 单调性

1. 快速连续发送普通 Prompt、命令和跟进消息。
2. 注入一个时间戳更靠后的服务器消息后再次发送，并切换会话重复。

**目视检查**

- 同一会话 ID 始终递增且顺序稳定；不同会话互不污染。
- 扩展 ID 使用兼容分隔格式，事件和历史投影均可识别。

### MT-09 完整与定点 Fork

1. 分别从某条消息处 Fork 和选择完整会话 Fork。
2. 在远程项目和含工具消息的长会话中重复。

**目视检查**

- 定点 Fork 截止于目标消息；完整 Fork 保留全部历史。
- 新会话位于正确服务器/项目并可继续发送消息。

### MT-10 已完成工作折叠

1. 构造多轮已完成工作和一轮正在运行的工作。
2. 展开/折叠历史组，刷新后继续对话。

**目视检查**

- 只有已完成历史进入分组，数量和累计时长正确；当前工作保持展开。
- 展开不会造成跳屏、顺序变化或子 Agent 链接失效。

### MT-11 压缩时间线

1. 触发一次有摘要的压缩，再测试无摘要和压缩后继续运行。
2. 刷新会话并在子 Agent 会话间往返。

**目视检查**

- 压缩边界只出现一次，压缩前后消息归属正确。
- 当前运行、锚点、虚拟列表高度和导航在刷新后保持稳定。

### MT-12 Reasoning 与 Shell

1. 运行包含长 reasoning 与长 Shell 输出的任务。
2. 流式期间向上滚动并手动切换展开状态，完成后再次展开。

**目视检查**

- Reasoning 流式时自动跟随，正文出现后摘要收起；用户操作不会被增量覆盖。
- Shell 实时输出、滚动条、复制按钮和完成后折叠均正常。

### MT-13 未读完成提醒与 Bark

1. 开启桌面与 Bark 提醒，离开正在运行的会话并等待完成。
2. 保持会话可见、重复接收完成状态，并配置一个失败地址。

**目视检查**

- 只有未读后台完成触发一次提醒；已查看或重复事件不提醒。
- Bark 成功发送；失败不会中断会话，并提供可诊断的错误结果。

### MT-14 终端焦点与后台运行

1. 新建终端，在 tmux 等程序中输入与应用快捷键重合的组合键。
2. 在终端内部节点和外部之间切换焦点，再最小化窗口数分钟。

**目视检查**

- 新终端自动聚焦，组合键交给 PTY；离开终端后应用快捷键恢复。
- 不出现宿主游离光标；后台终端输出和流式事件持续处理。

### MT-15 断线恢复与协议重探测

1. 让服务器不可达，并以不同协议实现重启同一地址。
2. 在不可用页面点击重试。

**目视检查**

- 目标服务器重新探测协议并恢复；其他服务器的项目、标签和同步状态不被重置。

### MT-16 构建与国际化

1. 运行 App/UI 语言键一致性测试、桌面皮肤构建和 models 缓存测试。
2. 使用 Electron 本地 zip 缓存执行离线打包准备。

**目视检查**

- 语言键、皮肤动态导入和资源检查通过。
- models 快照按环境变量与缓存策略加载；桌面构建可复用本地缓存。

## 上游同步注意事项

- 合并时以最新 `dev` 的组件结构、协议类型和公共 API 为主，在对应职责点重新接入魔改行为，避免恢复已被上游替换的旧实现。
- 标题栏滚动应保留上游当前标签访问与焦点行为；皮肤只通过稳定变量和桌面作用域覆盖视觉。
- 会话时间线必须保留上游消息投影、虚拟列表和压缩语义，再叠加已完成工作分组与 reasoning 展示状态。
- Prompt 与跟进模式应使用当前协议的显式 delivery vocabulary，不通过旧的本地队列或隐式布尔值回退。
- 通知点击、外部链接、窗口主题和原生按钮调用应继续使用最新 desktop preload/main API。
- Schema、Protocol、Core、Server 与 Client 的运行时依赖方向必须保持仓库约束；生成文件只能通过对应生成脚本更新。

## 压缩前提交对应表

| 原提交 | 标题 | 对应改动 |
| --- | --- | --- |
| `1db6c88501` | `feat(desktop): add customizable workspace experience` | 汇总早期桌面工作区、皮肤、侧栏、恢复、会话切换、终端和构建改动。 |
| `f8429030a8` | `fix(app): support extended timestamp IDs` | 支持扩展时间戳消息 ID 并统一生成与观察逻辑。 |
| `71785ca7a4` | `feat(desktop): improve titlebar tab scrolling` | 重构标题栏标签溢出、滚动和皮肤兼容样式。 |
| `c971147093` | `feat(app): improve slash command suggestions` | 增加 Slash 建议说明和弹层定位能力。 |
| `3c03d2145c` | `feat(app): support full session forks` | 增加完整会话 Fork 模式。 |
| `c6c245ad0a` | `fix(app): simplify extended ID separator` | 简化扩展 ID 分隔符并同步 Schema/事件解析。 |
| `d09cba8868` | `feat(app): collapse completed work history` | 折叠已完成工作并显示数量与累计时长。 |
| `9b0771d69a` | `feat(desktop): add unread completion reminders` | 增加未读桌面通知、Bark 与提醒调度。 |
| `9f93deafbd` | `fix(desktop): improve compaction timeline` | 修正压缩边界、时间线投影与历史分组。 |
| `462390e766` | `fix(app): preserve running shell collapse` | 保持运行 Shell 的生命周期折叠状态。 |
| `5941bae2b0` | `fix(app): preserve ref-counted context lifetime` | 延迟释放引用计数资源，支持快速重新挂载。 |
| `fbde6d6d7c` | `fix(desktop): focus on terminal when launching` | 新建终端后立即聚焦。 |
| `595d7de493` | `fix(desktop): align new session panel bounds` | 对齐新建任务面板与桌面工作区边界。 |
| `9e9afaf157` | `feat(app): restore queued follow-ups` | 恢复引导/排队跟进设置与提交语义。 |
| `1d3d460509` | `feat(app): refine composer controls` | 调整 Composer 选择器、状态与布局。 |
| `7c510f8771` | `feat(desktop): add DeepSeek Harness skin` | 增加 DeepSeek Harness 皮肤、资源与构建校验。 |
| `3b4e089be3` | `fix(app): collapse streaming reasoning summary` | 稳定流式 reasoning 摘要的自动折叠与用户展开状态。 |
